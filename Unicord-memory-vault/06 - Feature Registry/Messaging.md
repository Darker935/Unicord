---
tags:
  - "#features"
aliases:
  - Messaging
---

# Messaging

- `ChannelPage` + `ChannelPageViewModel` load history, send, edit, attachments
- Message templates via `MessageTemplateSelector`
- Markdown renderer under `Controls/Markdown/`
- Unreads / read state: DSharpPlus read states + `ReadStateViewModel`

Not the current hot path. Do not refactor messaging while fixing voice unless the user asks.

---

## Links
- [[Feature Index]]
